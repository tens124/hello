<?xml version="1.0" encoding="UTF-8"?>
<web-app version="2.5" xmlns="http://java.sun.com/xml/ns/javaee"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://java.sun.com/xml/ns/javaee https://java.sun.com/xml/ns/javaee/web-app_2_5.xsd">

	<!-- 한글 인코딩 설정 : post방식. url에 데이터를 직접 입력하여 전송하지 않음 -->
	<!-- 이 필터는 필터를 통과하는 모든 요청에 UTF-8로의 강제 인코딩을 적용시킴 -->
	<!-- 필터 매핑이 /*로 되어 있기에, 프로젝트에 존재하는 모든 요청이 이 필터를 거치게 된다. 즉, 한글이나 특수기호가 깨질 일을 방지해줌 -->
	<filter>
		<filter-name>CharacterEncodingFilter</filter-name>
		<filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
		<init-param>
			<param-name>encoding</param-name>
			<param-value>UTF-8</param-value>
		</init-param>
		<init-param>
			<param-name>forceEncoding</param-name>
			<param-value>true</param-value>
		</init-param>
	</filter>
	<filter-mapping>
		<filter-name>CharacterEncodingFilter</filter-name>
		<url-pattern>/*</url-pattern>
	</filter-mapping>


	<!-- The definition of the Root Spring Container shared by all Servlets and Filters -->
	<context-param>
		<param-name>contextConfigLocation</param-name>
		<param-value>/WEB-INF/spring/root-context.xml
					 /WEB-INF/spring/spring-security.xml
		</param-value>
	</context-param>
	
	<!-- Creates the Spring Container shared by all Servlets and Filters -->
	<!-- 리스너 : 웹 애플리케이션에서 발생하는 특정 이벤트를 감지하고, 그에 대한 동작을 수행하는 컴포넌트 
	web.xml에 설정된 리스너는 애플리케이션의 시작, 종료, 세션 생성/소멸 등과 같은 라이프사이클 이벤트를 처리하는 데 사용-->
	<listener>
		<!-- ContextLoaderListener : 애플리케이션 시작 시 루트 컨텍스트를 생성하고 초기화하는 역할
			 Spring 설정 파일(contextConfigLocation)을 로드
			 루트 컨텍스트에 정의된 빈을 생성 및 초기화
			 서블릿 컨텍스트와 별도로 전역적으로 사용할 빈 관리(서블릿은 각각의 빈 컨테이너를 따로 가지고 있음) -->
		<listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
	</listener>

	<!-- Processes application requests -->
	<servlet>
		<servlet-name>appServlet</servlet-name>
		<!-- 디스패쳐 서블릿 : 프론트 컨트롤러. 요청 발생 시 가장 먼저 반응 -->
		<servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
		<init-param>
			<param-name>contextConfigLocation</param-name>
			<param-value>/WEB-INF/spring/appServlet/servlet-context.xml</param-value>
		</init-param>
		<load-on-startup>1</load-on-startup>
	</servlet>
		
	<servlet-mapping>
		<servlet-name>appServlet</servlet-name>
		<url-pattern>*.do</url-pattern>
	</servlet-mapping>

</web-app>
